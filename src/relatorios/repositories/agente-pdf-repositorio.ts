export default class AgentePdfRepository {

  public async gerar(agentes:any): Promise<any> {

    try {

    } catch (e) {
      console.log(e);
      return Error('Não foi possível gerar o relatório.');
    }

  }

  
}
